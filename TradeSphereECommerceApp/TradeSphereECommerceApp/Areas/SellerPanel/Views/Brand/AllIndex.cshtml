@model IEnumerable<TradeSphereECommerceApp.Models.Brand>
@{
    ViewBag.Title = "AllIndex";
    Layout = "~/Areas/SellerPanel/Views/Shared/_LayoutPage1.cshtml";
}

<div class="breadCrumb">
    <a href="@Url.Action("Index", "Home")">Anasayfa</a> >
    <label>Markalar</label>
</div>

<div class="pageContent">
    <div class="pagetitle">
        @Html.ActionLink("Marka Oluştur", "Create")
        <h2>Markalar</h2>
        <div style="clear:both"></div>
    </div>
    <div class="tableContent">
        <table class="table">
            <tr>
                <th style="border-left:none">
                    Marka No
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IsActive)
                </th>
                <th>Seçenekler</th>
            </tr>

            @foreach (var item in Model)
            {
                if (item.IsDeleted)
                {
                    <tr style="background-color:#f33636">
                        <td style="border-left:none">@item.ID</td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @if (item.IsActive)
                            {
                                <label>Aktif</label>
                            }
                            else
                            {
                                <label>Pasif</label>
                            }
                        </td>
                        <td>
                            <a href="@Url.Action("Edit","Brand",new {id=item.ID})" class="btnedit">Düzenle</a>
                            <a href="@Url.Action("ReDelete","Brand",new {id=item.ID})" class="btndelete">Silme İşlemini Geri Al</a>

                        </td>
                    </tr>
                }
                else
                {
                    <tr>
                        <td style="border-left:none">@item.ID</td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @if (item.IsActive)
                            {
                                <label>Aktif</label>
                            }
                            else
                            {
                                <label>Pasif</label>
                            }
                        </td>
                        <td>
                            <a href="@Url.Action("Edit","Brand",new {id=item.ID})" class="btnedit">Düzenle</a>
                            <a href="@Url.Action("Delete","Brand",new {id=item.ID})" class="btndelete">Sil</a>
                        </td>
                    </tr>
                }
            }
        </table>

    </div>
    <div class="tablefooter">
        <a href="@Url.Action("Index","Brand")">Markaları Listele</a>
    </div>
</div>

@section styles
    {
    <link href="~/Areas/ManagerPanel/Assests/css/TableStyle.css" rel="stylesheet" />
}

